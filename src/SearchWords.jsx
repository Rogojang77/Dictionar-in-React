import React, { useState } from 'react';

export default function SearchWords() {
    return (
    <div>
        <input placeholder="Search word in dictionary"></input>
        <button >Search</button>
    </div>
  )
}